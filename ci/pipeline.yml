resources:
- name: flight-school
  type: git
  source:
    uri: https://github.com/sample/flight-school
    branch: master
    branches: test-branch

jobs:
- name: test-app
  plan:
  - get: flight-school
    trigger: true
  - task: tests
    file: flight-school/build.yml
